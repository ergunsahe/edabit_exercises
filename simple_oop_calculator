""" in this exercise we can assign a class to a variable and then use class metods with dot notation. calculator=Calculator()==> calculator.add(10,5)==>15"""
class Calculator:
	def add(self, x, y):
		return x + y
	def subtract(self, x, y):
		return x - y
	def multiply(self, x, y):
		return x * y
	def divide(self, x, y):
		return x / y
